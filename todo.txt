
-text label anywhere on the canvas
-reset to default size selected node(s)
-ctrl selection
-settings panel
-documentation and help links about SDL (https://github.com/csurgay/sdl)
-movement on the canvas
-zoom & grab canvas  
  
-long name breaks in nodes
-save diagram as png

-better save JSON format for future interpreter (multi process mode defined by start nodes)
-SDL code convert from JSON 
-SDL interpreter

DONE:
2025.11.14:
-editable node labels
2025.11.15:
-connection points only when selected/hover
-edge select and delete
-no text selection
-[BUG] while editing a label, backspace shouldnt delete the node
-load button -> panel with saved diagrams
-save naming without pop up default window
-[BUG] termination, not proper connection edges in some cases (SOLVED by putting all 4 ports in the center, kinda lame)
-only 2 edges from one node (in&out), except decision
2025.11.26:
-clear button
-proper connection edges (simplified)
-unique IDs after loading previous diagrams
2025.11.27:
-undo/redo (Ctrl+Z / Ctrl+Y)
2025.12.02:
-unique IDs bug solved  
-arrow into states
-better code structure
-connections validations (start node → max 1 outgoing edge, no self-connections, no duplicate edges between two nodes)
-decision different branching names with save/load 